// src/app/interfaces/shared/audit-user.interface.ts
// Generado una sola vez — reusada por TODOS los módulos para created_by_id/updated_by_id/deleted_by_id.
// Ver Interfaz de Modulo.md#Campos de auditoría. No se regenera por módulo.

export interface IAuditUser {
  id: string;
  nombre: string;
}
