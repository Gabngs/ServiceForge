# Python
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.venv/
venv/

# PyInstaller
build/
dist/
*.spec.bak

# Version pineada por CI al construir un release (ver __init__.py) — nunca
# se commitea, se genera sola en cada build de un tag v*.
src/generador/_version.py

# Backups generados por la app (nunca committear un dump real)
*.sql

# IDE
.vscode/
.idea/

# Dataset de estructura: los esqueletos sintéticos son efímeros (se borran solos al construir el dataset),
# el CSV duplica al parquet y los PNG se regeneran con scripts/make_dataset_stats.py
datasets/structure/synthetic/
datasets/structure/pairs.csv
datasets/structure/stats/*.png
